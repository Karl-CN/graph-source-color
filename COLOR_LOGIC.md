# Graph Source Color — 着色逻辑文档

基于当前代码（回滚后的原始正向 BFS 版本），完整梳理从源点到最终颜色的全过程。

---

## 核心数据结构

| 结构 | 类型 | 含义 |
|------|------|------|
| `sourceCache` | `Map<路径, SourceInfo>` | 所有源点笔记，key 是文件路径 |
| `childMap` | `Map<路径, Set<源点路径>>` | 每个笔记关联了哪些源点（key 是笔记路径，value 是源点路径集合） |

一个笔记的最终颜色 = `childMap[笔记路径]` 中所有源点的颜色集合。

---

## 步骤 1：识别源点

**逻辑：** 遍历 Vault 所有 markdown 文件，如果文件路径以 `sourceFolders` 中的某个文件夹开头，则该文件是源点。

**规则：**
- `group` = 最长匹配的源点文件夹路径
- `color` = `groupColors[group]`，如果该 group 没有设色，向上查找父文件夹的颜色（递归继承）

**当前配置：**
- sourceFolders = `["wiki/sources", "wiki/analysis"]`
- groupColors = `{"wiki/sources": "#0ac6f5", "wiki/analysis": "#8700f5"}`

**识别出的源点：**

| 源点文件 | group | 代码中颜色 | Obsidian手动设色（覆盖） |
|----------|-------|-----------|------------------------|
| `wiki/sources/行业分类标准.md` | wiki/sources | #0ac6f5 | #f5f906 |
| `wiki/sources/2026年政府工作报告.md` | wiki/sources | #0ac6f5 | #fe011b |
| `wiki/sources/2026年4月政治局经济工作会议.md` | wiki/sources | #0ac6f5 | 无 → 用 #0ac6f5 |

> **注意：** 源点的最终显示颜色在渲染阶段由 `getEffectiveSourceColor()` 决定，优先用 Obsidian 图谱手动设色，其次用 groupColors。

---

## 步骤 2：源点出链建立子节点映射

**逻辑：** 对每个源点，遍历它的出链。如果出链目标**不是源点**，则把目标加入 `childMap`，关联到该源点。

**方向：** 源点 → 出链目标（源点链接了谁，谁就是子节点）

**示例：**
```
行业分类标准.md 的出链 → 钢铁.md, 食品饮料.md, ...
  → childMap["钢铁.md"].add("行业分类标准.md")
  → childMap["食品饮料.md"].add("行业分类标准.md")

4月政治局经济工作会议.md 的出链 → 新质生产力.md, 算力网.md, ...
  → childMap["新质生产力.md"].add("4月政治局经济工作会议.md")
  → childMap["算力网.md"].add("4月政治局经济工作会议.md")
```

**排除规则：** 源点之间的互链不建立关联（`if sourceCache.has(resolvedPath) continue`）

---

## 步骤 2b：非源点笔记链接源点，反向建立关联

**逻辑：** 对每个非源点笔记，遍历它的出链。如果出链目标是**源点**，则把该笔记自己加入 `childMap`，关联到那个源点。

**方向：** 非源点 → 源点（谁链接了源点，谁就是子节点）

**示例：**
```
新质生产力.md 的出链包含 → 2026年政府工作报告.md（源点）
  → childMap["新质生产力.md"].add("2026年政府工作报告.md")

新质生产力.md 的出链包含 → 行业分类标准.md（源点）
  → childMap["新质生产力.md"].add("行业分类标准.md")
```

**此时新质生产力的 childMap：**
```
"新质生产力.md" → {
  "4月政治局经济工作会议.md",  // 来自步骤2
  "2026年政府工作报告.md",      // 来自步骤2b
  "行业分类标准.md"             // 来自步骤2b
}
```
→ 新质生产力 = 三色 ✅

**排除规则：**
- 源点自身不参与
- `wiki/index.md` 被显式排除

---

## 步骤 3：正向 BFS 传播

**逻辑：** 对 `childMap` 中已有源点关联的节点，沿其出链继续传播源点关系到下游节点。只走**正向**（出链方向）。

**规则：**
- 起始队列 = childMap 中所有已有源点关联的节点
- 对队列中每个节点，遍历其出链：
  - 目标是源点 → 跳过
  - 目标不是源点 → 将当前节点的所有源点关联复制给目标
  - 如果目标获得了新源点，加入队列继续传播
- `visited` 集合防止同一节点重复入队（即使后续有新源点也不再传播）

**示例：**
```
新质生产力.md (3色) 的出链 → 半导体.md, 航空装备Ⅱ.md, ...
  → 半导体.md 获得新质生产力的3个源点 → 加入队列
  → 航空装备Ⅱ.md 获得新质生产力的3个源点？

关键问题：此时航空装备Ⅱ可能已经在步骤2/2b中获得了部分源点，
如果它已经被 visited，新质生产力传来的额外源点会被写入 childMap，
但航空装备Ⅱ不会再入队传播给它的出链目标。
```

**⚠️ visited 的限制：** 一个节点只在**首次被加入队列时**标记 visited。如果它后来又被其他节点追加了新源点，虽然 childMap 会更新，但该节点不会重新入队传播。

---

## 渲染阶段：从 childMap 到最终颜色

**逻辑（main.ts 中 `getChildNodeColors`）：**

1. 从 `childMap` 获取笔记关联的所有源点路径
2. 对每个源点，调用 `getEffectiveSourceColor()` 获取颜色：
   - 先查 Obsidian 图谱手动设色（`obsidianColorMap`）
   - 没有手动设色 → 用 `sourceDetector.getSourceInfo()` 中的颜色
3. 去重：相同颜色只保留一个

**颜色去重问题：**
- 行业分类标准和4月政治局经济工作会议，groupColors 都是 `wiki/sources` → 代码颜色都是 `#0ac6f5`
- 但行业分类标准有 Obsidian 手动设色 `#f5f906`，政府工作报告有手动设色 `#fe011b`
- 4月政治局经济工作会议没有手动设色，最终用 `#0ac6f5`

所以3个源点最终3种颜色：`#f5f906`, `#fe011b`, `#0ac6f5` ✅ 不去重

---

## 航空装备Ⅱ 的完整追踪

**文件路径：** `wiki/industries/国防军工/航空装备Ⅱ.md`

**航空装备Ⅱ的出链：**
- → 国防军工（父行业节点，非源点）
- → 新质生产力（概念节点，非源点）

**步骤2（源点出链）：**
- 行业分类标准的出链 → 是否指向航空装备Ⅱ？需看实际笔记内容
- 政府工作报告的出链 → 是否指向航空装备Ⅱ？需看实际笔记内容
- 4月政治局经济工作会议的出链 → 是否指向航空装备Ⅱ？需看实际笔记内容
- 新质生产力的出链 → 包含航空装备Ⅱ → **但新质生产力不是源点，步骤2不处理**

**步骤2b（非源点→源点反向）：**
- 航空装备Ⅱ的出链 → 国防军工（非源点）→ 不处理
- 航空装备Ⅱ的出链 → 新质生产力（非源点）→ 不处理
- **结论：步骤2b不会给航空装备Ⅱ建立任何关联**

**步骤3（正向BFS）：**
- 新质生产力在队列中（3个源点），出链包含航空装备Ⅱ
- 航空装备Ⅱ获得新质生产力的3个源点
- **但如果航空装备Ⅱ在步骤2中已被某个源点关联过，visited 已标记，不会再入队**

**当前调试输出：** 航空装备Ⅱ = `["#f5f906","#fe011b"]`（双色）

**缺少 #0ac6f5 的可能原因：**
- 4月政治局经济工作会议的出链不包含航空装备Ⅱ
- 而新质生产力虽然出链包含航空装备Ⅱ，但传播时因为 visited 限制，可能没有完整传入

---

## 逻辑总结

```
源点识别 ───→ sourceCache（哪些文件是源点）
     │
     ▼
步骤2：源点──出链──→ 非源点        childMap[目标] += 源点
     │
     ▼
步骤2b：非源点──出链──→ 源点       childMap[发起者] += 源点
     │
     ▼
步骤3：已有颜色的节点──出链──→ 下游   childMap[下游] += 发起者的所有源点
     │              （只正向，visited 限制只入队一次）
     ▼
渲染：childMap → 源点颜色（Obsidian手动 > groupColors）→ 去重 → 绘制节点
```

**核心局限：**
1. 步骤2/2b 只处理"源点↔非源点"的直接关系，不处理"非源点→已着色非源点"
2. 步骤3 只沿出链正向传播，不沿入链反向传播
3. visited 限制导致节点只能入队一次，后续追加的源点不会再传播
